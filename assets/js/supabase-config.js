/**
 * OFF-LIMITS ENTERTAINMENT — Supabase connection config
 *
 * 1. Create a free project at https://supabase.com
 * 2. Project Settings → API → copy "Project URL" and "anon public" key
 * 3. Paste them below. The anon key is safe to expose in client-side code —
 *    Supabase Row Level Security (see supabase/schema.sql) controls what it
 *    can actually read/write.
 */
window.SUPABASE_URL = 'YOUR_SUPABASE_PROJECT_URL';
window.SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
